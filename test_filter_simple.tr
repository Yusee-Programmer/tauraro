#!/usr/bin/env tauraro
# Simple list to filter
numbers = [1, 2, 3, 4, 5]

# Try a very simple filter
print("Calling filter...")
try:
    result = filter(lambda x: True, numbers)
    print(f"Filter returned: {result}")
    print(f"Result type: {type(result)}")
except Exception as e:
    print(f"Error in filter: {e}")

print("Done")
