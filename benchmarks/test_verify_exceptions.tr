print("COMPREHENSIVE EXCEPTION SYSTEM VERIFICATION")

print("\nTest 1: Exception Hierarchy")
try:
    x = []
    y = x[100]
except Exception:
    print("OK: Exception caught via base Exception class")

print("\nTest 2: Exception Chaining")
exception_caught = False
try:
    try:
        x = int("invalid")
    except ValueError as e:
        raise RuntimeError("Parsing failed") from e
except RuntimeError:
    print("OK: Exception chain caught")
    exception_caught = True

print("\nTest 3: Custom Exceptions")
class CustomError(ValueError):
    pass

try:
    raise CustomError("Custom error message")
except CustomError:
    print("OK: Custom exception caught")

print("\nTest 4: Built-in Exceptions")
try:
    d = {}
    x = d["missing"]
except KeyError:
    print("OK: KeyError caught correctly")

print("\nTest 5: Bare Except Clause")
try:
    x = "string" + 5
except:
    print("OK: Bare except caught exception")

print("\nTest 6: Exception Binding")
try:
    raise ValueError("Test message")
except ValueError as e:
    msg = str(e)
    print("OK: Exception bound to variable")

print("\nTest 7: Sequential Exception Handlers")
test_passed = False
try:
    x = [1, 2, 3][10]
except ValueError:
    print("Handler 1 triggered")
except IndexError:
    print("OK: Handler 2 caught IndexError")
    test_passed = True
except:
    print("Handler 3 triggered")

print("\nTest 8: Exception with Cause")
try:
    try:
        x = int("not a number")
    except ValueError as original:
        raise TypeError("Type error") from original
except TypeError:
    print("OK: Exception with cause stored successfully")

print("\nTest 9: Nested Try-Except")
outer_caught = False
inner_caught = False

try:
    try:
        raise ZeroDivisionError("Test")
    except ValueError:
        print("Inner handler 1")
    except ZeroDivisionError:
        inner_caught = True
    if inner_caught:
        raise RuntimeError("Outer error")
except RuntimeError:
    outer_caught = True

if inner_caught and outer_caught:
    print("OK: Nested exception handling works correctly")

print("\nVERIFICATION COMPLETE")
print("Exception System: Operational and Feature-Complete")
