pub class Box[T]:
    pub val: T

extend Box[T]:
    pub def init(v: T) -> Box[T]:
        mut b = Box[T]()
        b.val = v
        return b

    pub def get(self) -> T:
        return self.val

def main():
    mut b = Box[int].init(42)
    print(b.get().to_str())
