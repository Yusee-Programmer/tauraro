# Debug __call__ issue - even simpler

class TestCall:
    def __call__(self, x):
        return x + 100

obj = TestCall()
print("Created object")
print(f"Calling obj(5)...")
try:
    result = obj(5)
    print(f"Result: {result}")
except Exception as e:
    print(f"Error: {e}")
