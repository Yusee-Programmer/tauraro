#!/usr/bin/env tauraro

# Test calling a closure function with *args

def outer(f):
    def inner(*args):
        print("About to call f")
        return f(*args)
    return inner

def target(x):
    return x * 2

wrapped = outer(target)
result = wrapped(5)
print(result)
